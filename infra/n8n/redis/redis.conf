# Redis hardened defaults for n8n queue
port 6379
bind 0.0.0.0
protected-mode yes

# Persistence off (queue is ephemeral; n8n handles retries)
save ""
appendonly no

# Memory and eviction
maxmemory 512mb
maxmemory-policy allkeys-lru

# Logging
databases 16
loglevel notice

